DATABASE_URL="postgresql://valentine:valentine@localhost:5432/valentine?schema=public"
PORT=3001
HOST=0.0.0.0

# 32+ bytes recommended. For local dev you can use any long random string.
ENCRYPTION_KEY="change-me"
ADMIN_PASSWORD="change-me"
ADMIN_JWT_SECRET="change-me"

# Optional bootstrap keys (can also be set via Admin Settings UI)
OPENAI_API_KEY=""
KAI_AI_KEY=""

# LLM provider settings
# - For OpenRouter, set:
#   OPENAI_BASE_URL="https://openrouter.ai/api/v1"
#   OPENAI_MODEL="<openrouter-model-id>"
# If OPENAI_BASE_URL is not set and OPENAI_API_KEY starts with "sk-or-", the app will default to OpenRouter automatically.
OPENAI_BASE_URL=""
OPENAI_MODEL=""
OPENAI_SYSTEM_PROMPT="You are a helpful assistant that follows instructions precisely."
# Optional OpenRouter attribution headers
OPENAI_DEFAULT_HEADERS_REFERER=""
OPENAI_DEFAULT_HEADERS_TITLE=""

# Kie.ai (Suno) settings
KIE_AI_BASE_URL="https://api.kie.ai"
KIE_AI_MODEL="V5"
# Required by Kie.ai generate endpoint. Must be a publicly reachable HTTPS URL.
KIE_AI_CALLBACK_URL=""

# SMTP (email verification)
SMTP_HOST=""
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="Laguin.id <no-reply@laguin.id>"

# Email provider (optional)
# - "resend": use Resend API (recommended)
# - "smtp": use SMTP via Nodemailer (legacy)
EMAIL_PROVIDER="resend"

# Resend (email verification)
# Get your API key at https://resend.com/api-keys
RESEND_API_KEY=""
# In dev you can use: "onboarding@resend.dev"
# In prod, use a sender from a verified domain in Resend.
RESEND_FROM="Laguin.id <no-reply@laguin.id>"

