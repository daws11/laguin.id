## Infra (Docker) environment
## Copy to `infra/.env` (optional) and adjust as needed.
##
## Default values are already provided in `docker-compose.yml`,
## so you can run without creating `infra/.env`.

# Postgres credentials / database name
POSTGRES_USER=valentine
POSTGRES_PASSWORD=valentine
POSTGRES_DB=valentine

# Host port exposed for Postgres on the server
POSTGRES_PORT=5432

